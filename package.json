{
  "name": "basalio-astro",
  "type": "module",
  "version": "0.5.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:clean": "rm -rf node_modules/.vite && npm run dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "verify:overflow": "node scripts/verify-section.js",
    "verify:headings": "node scripts/verify-headings.mjs",
    "pre-deploy": "bash scripts/pre-deploy.sh"
  },
  "dependencies": {
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
