{
  "name": "basalio-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:clean": "rm -rf node_modules/.vite && npm run dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "verify:overflow": "node scripts/verify-section.js",
    "verify:headings": "node scripts/verify-headings.mjs",
    "pre-deploy": "npm run verify:overflow && npm run build"
  },
  "dependencies": {
    "astro": "^7.0.7",
    "gsap": "^3.15.0",
    "wcag-contrast": "^3.0.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
